\documentclass{article}
\usepackage{graphicx} % Required for inserting images

\title{Normalization}

\begin{document}

\maketitle


&((\lambda a.(\lambda b.b\ b)(\lambda b.b\ b))\ b)
  ((\lambda c.(c\ b))(\lambda a.a))

&\to_\beta
((\lambda b.b\ b)(\lambda b.b\ b))
  ((\lambda c.(c\ b))(\lambda a.a))
  
&\to_\beta
((\lambda b.b\ b)(\lambda b.b\ b))
  ((\lambda c.(c\ b))(\lambda a.a))

&\to_\beta
((\lambda b.b\ b)(\lambda b.b\ b))
  ((\lambda c.(c\ b))(\lambda a.a))

&\to_\beta \ldots

\text{As a result, there is no normal form.}


\end{document}
